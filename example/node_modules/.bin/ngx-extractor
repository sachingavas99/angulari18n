#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ngx-translate/i18n-polyfill/extractor/ngx-extractor.js" "$@"
  ret=$?
else 
  node  "$basedir/../@ngx-translate/i18n-polyfill/extractor/ngx-extractor.js" "$@"
  ret=$?
fi
exit $ret
